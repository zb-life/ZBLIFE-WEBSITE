const json=(statusCode,body)=>({statusCode,headers:{'content-type':'application/json'},body:JSON.stringify(body)});
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function sendEmail(apiKey,payload){const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${apiKey}`,'content-type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw new Error(await r.text());return r.json()}
function shippingHtml(order){const link=order.trackingUrl?`<p style="margin:24px 0"><a href="${esc(order.trackingUrl)}" style="display:inline-block;background:#171717;color:white;text-decoration:none;padding:12px 18px;border-radius:999px">Track your order</a></p>`:'';return `<div style="font-family:Arial,sans-serif;color:#181818;max-width:640px;margin:auto"><h1 style="font-size:26px;letter-spacing:.08em">ZIONBURG</h1><p>Your order <strong>${esc(order.id)}</strong> has shipped.</p><div style="background:#f6f6f6;border-radius:14px;padding:18px;margin:22px 0"><p style="margin:0 0 8px">Carrier: <strong>${esc(order.trackingCarrier||'')}</strong></p><p style="margin:0">Tracking number: <strong>${esc(order.trackingNumber||'')}</strong></p></div>${link}<p style="color:#666">Thank you for shopping with ZIONBURG.</p></div>`}
export async function handler(event){if(event.httpMethod!=='POST')return json(405,{error:'Method not allowed'});const apiKey=process.env.RESEND_API_KEY,from=process.env.ZB_FROM_EMAIL;if(!apiKey||!from)return json(503,{error:'Email service is not configured'});let body;try{body=JSON.parse(event.body||'{}')}catch{return json(400,{error:'Invalid JSON'})}const order=body.order;if(!order?.id||!order?.email||!order?.trackingNumber)return json(400,{error:'Order, customer email and tracking number are required'});try{await sendEmail(apiKey,{from,to:[order.email],subject:`Your ZIONBURG order has shipped — ${order.id}`,html:shippingHtml(order)});return json(200,{ok:true})}catch(err){return json(502,{error:'Email send failed',detail:String(err.message||err).slice(0,500)})}}

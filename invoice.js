const e=s=>String(s??'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));document.addEventListener('DOMContentLoaded',()=>{const q=new URLSearchParams(location.search),orderId=q.get('order'),invoiceId=q.get('invoice');if(orderId){const o=JSON.parse(localStorage.getItem(ZB_ORDERS)||'[]').find(x=>x.id===orderId);if(!o){invoiceSheet.innerHTML='<h1>Order not found</h1>';return}invoiceSheet.innerHTML=`<div class="invoice-head"><div><span class="eyebrow">ORDER RECEIPT</span><h1>${e(o.id)}</h1></div><div class="invoice-total">${money(o.total)}</div></div><p class="receipt-meta">${new Date(o.date).toLocaleString()} • ${e(o.customer)} • ${e(o.email)}</p>${o.items.map(i=>`<div class="order-item"><span>${e(i.name)}<br><small>${e(i.detail||'')}</small></span><b>${money(i.price*i.qty)}</b></div>`).join('')}<div class="totals"><div><span>Subtotal</span><b>${money(o.subtotal)}</b></div>${o.discount?`<div><span>Discount</span><b>−${money(o.discount)}</b></div>`:''}<div><span>Shipping</span><b>${o.shipping?money(o.shipping):'FREE'}</b></div><div class="grand"><span>Total paid</span><b>${money(o.total)}</b></div></div><div class="code-note" style="margin-top:25px">Prototype receipt — no live payment was processed.</div>`;return}const arr=JSON.parse(localStorage.getItem(ZB_INVOICES)||'[]'),inv=arr.find(x=>x.id===invoiceId);if(!inv){invoiceSheet.innerHTML='<h1>Invoice not found</h1>';return}const isPaid=inv.status==='Paid';invoiceSheet.innerHTML=`<div class="invoice-head"><div><span class="eyebrow">CUSTOM INVOICE</span><h1>${e(inv.id)}</h1></div><div class="invoice-total">${money(inv.total)}</div></div><p class="receipt-meta">${e(inv.customer)} • ${e(inv.email)}${inv.due?' • Due '+e(inv.due):''}${inv.reference?' • Ref '+e(inv.reference):''}</p>${inv.lines.map(l=>`<div class="order-item"><span>${e(l.description)} × ${l.qty}</span><b>${money(l.qty*l.price)}</b></div>`).join('')}<div class="totals"><div><span>Subtotal</span><b>${money(inv.subtotal)}</b></div><div><span>Shipping</span><b>${money(inv.shipping)}</b></div>${inv.discount?`<div><span>Discount</span><b>−${money(inv.discount)}</b></div>`:''}<div class="grand"><span>Total</span><b>${money(inv.total)}</b></div></div>${inv.notes?`<p class="receipt-meta">${e(inv.notes)}</p>`:''}<div class="invoice-pay">${isPaid?'<div class="success-banner show">Payment recorded in this prototype.</div>':'<button class="btn btn-dark full" id="payInvoice">PAY '+money(inv.total)+' →</button>'}</div><div class="code-note" style="margin-top:20px">Prototype payment link. A live build would redirect to or embed Stripe here.</div>`;const b=document.getElementById('payInvoice');if(b)b.onclick=()=>{inv.status='Paid';localStorage.setItem(ZB_INVOICES,JSON.stringify(arr));if(inv.email)logEmailEvent({type:'Customer paid receipt',to:inv.email,subject:`Payment receipt — ${inv.id}`,invoiceId:inv.id});salesEmailList(getSite()).forEach(to=>logEmailEvent({type:'Sales paid invoice copy',to,subject:`Paid invoice copy — ${inv.id}`,invoiceId:inv.id}));fetch('/.netlify/functions/send-invoice-email',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({invoice:inv,url:location.href,event:'paid'})}).catch(()=>{});location.reload()}})

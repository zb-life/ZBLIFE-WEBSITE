# Example only.
# If `supabase init` has already created supabase/config.toml in your repo,
# KEEP the generated file and do not overwrite it blindly.

project_id = "zionburg-website"

[db.seed]
enabled = true
sql_paths = ["./seed.sql"]

# If your generated config already contains an [experimental.pgdelta] section,
# keep the value generated by your installed Supabase CLI.
